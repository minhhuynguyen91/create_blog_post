<%= form_for @blog, :html => {class: 'ui form'} do |f| %>
  <div class="field">
    <label> Title </label> 
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <label> Body </label> 
    <%= f.text_area :content %> <br>
  </div>
  <%= f.submit 'Submit', class: 'ui button' %>
<% end %>